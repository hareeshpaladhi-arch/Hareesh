package com.ai.classification.repository;

import com.ai.classification.entity.AlterWord;
import org.springframework.data.jpa.repository.JpaRepository;

public interface AlterWordRepository extends JpaRepository<AlterWord, Long> {
}
